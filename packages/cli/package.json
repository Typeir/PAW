{
  "name": "@paw/cli",
  "version": "0.0.0",
  "private": true,
  "description": "PAW CLI — a driving adapter that consumes @paw/core. Holds no domain logic.",
  "type": "module",
  "bin": {
    "paw": "./src/main.ts"
  },
  "scripts": {
    "test": "vitest run",
    "test:cov": "vitest run --coverage",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@paw/daemon": "file:../daemon",
    "@paw/adapters": "file:../adapters",
    "@paw/core": "file:../core"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^2.1.9",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3",
    "vitest": "^2.1.9"
  }
}
